diff --git a/ClinicFlow.Domain/Entities/Clinic.cs b/ClinicFlow.Domain/Entities/Clinic.cs
index 2485b1c..1d6124b 100644
--- a/ClinicFlow.Domain/Entities/Clinic.cs
+++ b/ClinicFlow.Domain/Entities/Clinic.cs
@@ -26,6 +26,8 @@
 
         public ICollection<Patient> Patients { get; set; } = new List<Patient>();
 
+        public ICollection<ClinicPatient> ClinicPatients { get; set; } = new List<ClinicPatient>();
+
         public ICollection<Invoice> Invoices { get; set; } = new List<Invoice>();
 
         public ICollection<ClinicWorkingHour> ClinicWorkingHours { get; set; } = new List<ClinicWorkingHour>();
diff --git a/ClinicFlow.Domain/Entities/Patient.cs b/ClinicFlow.Domain/Entities/Patient.cs
index c6f1fd4..a507240 100644
--- a/ClinicFlow.Domain/Entities/Patient.cs
+++ b/ClinicFlow.Domain/Entities/Patient.cs
@@ -7,20 +7,12 @@ namespace ClinicFlow.Domain.Entities
 
         public int Id { get; set; }
 
-        public int ClinicId { get; set; }
-
-        public string FirstName { get; set; } = null!;
-
-        public string LastName { get; set; } = null!;
+        public int PersonId { get; set; }
 
         public DateOnly DateOfBirth { get; set; }
 
         public GenderEnum Gender { get; set; }
 
-        public string PhoneNumber { get; set; } = null!;
-
-        public string? Email { get; set; }
-
         public string? Notes { get; set; }
 
         public string? Address { get; set; }
@@ -35,7 +27,11 @@ namespace ClinicFlow.Domain.Entities
 
         public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
 
-        public Clinic Clinic { get; set; } = null!;
+        public DateTime? UpdatedAt { get; set; }
+
+        public Person Person { get; set; } = null!;
+
+        public ICollection<ClinicPatient> ClinicPatients { get; set; } = new List<ClinicPatient>();
 
         public ICollection<Appointment> Appointments { get; set; } = new List<Appointment>();
 
diff --git a/ClinicFlow.Domain/Entities/User.cs b/ClinicFlow.Domain/Entities/User.cs
index 3ea5cee..315286c 100644
--- a/ClinicFlow.Domain/Entities/User.cs
+++ b/ClinicFlow.Domain/Entities/User.cs
@@ -7,11 +7,9 @@ namespace ClinicFlow.Domain.Entities
 
         public int Id { get; set; }
 
-        public int ClinicId { get; set; }
-
-        public string FirstName { get; set; } = null!;
+        public int PersonId { get; set; }
 
-        public string LastName { get; set; } = null!;
+        public int ClinicId { get; set; }
 
         public string Email { get; set; } = null!;
 
@@ -23,6 +21,10 @@ namespace ClinicFlow.Domain.Entities
 
         public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
 
+        public DateTime? UpdatedAt { get; set; }
+
+        public Person Person { get; set; } = null!;
+
         public Clinic Clinic { get; set; } = null!;
 
         public ICollection<UserRole> UserRoles { get; set; } = new List<UserRole>();
diff --git a/ClinicFlow.Infrastructure/Data/AppDbContext.cs b/ClinicFlow.Infrastructure/Data/AppDbContext.cs
index e6a26c1..5dd863a 100644
--- a/ClinicFlow.Infrastructure/Data/AppDbContext.cs
+++ b/ClinicFlow.Infrastructure/Data/AppDbContext.cs
@@ -14,6 +14,8 @@ namespace ClinicFlow.Infrastructure.Data
 
         public DbSet<Clinic> Clinics => Set<Clinic>();
 
+        public DbSet<Person> Persons => Set<Person>();
+
         public DbSet<User> Users => Set<User>();
 
         public DbSet<Role> Roles => Set<Role>();
@@ -28,6 +30,8 @@ namespace ClinicFlow.Infrastructure.Data
 
         public DbSet<Patient> Patients => Set<Patient>();
 
+        public DbSet<ClinicPatient> ClinicPatients => Set<ClinicPatient>();
+
         public DbSet<Appointment> Appointments => Set<Appointment>();
 
         public DbSet<MedicalRecord> MedicalRecords => Set<MedicalRecord>();
diff --git a/ClinicFlow.Infrastructure/Data/Config/ClinicConfigration.cs b/ClinicFlow.Infrastructure/Data/Config/ClinicConfigration.cs
index 850e3f6..3f47d5e 100644
--- a/ClinicFlow.Infrastructure/Data/Config/ClinicConfigration.cs
+++ b/ClinicFlow.Infrastructure/Data/Config/ClinicConfigration.cs
@@ -64,9 +64,9 @@ namespace ClinicFlow.Infrastructure.Data.Config
                 .HasForeignKey(d => d.ClinicId)
                 .OnDelete(DeleteBehavior.Restrict);
 
-            builder.HasMany(c => c.Patients)
-                .WithOne(p => p.Clinic)
-                .HasForeignKey(p => p.ClinicId)
+            builder.HasMany(c => c.ClinicPatients)
+                .WithOne(cp => cp.Clinic)
+                .HasForeignKey(cp => cp.ClinicId)
                 .OnDelete(DeleteBehavior.Restrict);
 
             builder.HasMany(c => c.Invoices)
@@ -74,6 +74,11 @@ namespace ClinicFlow.Infrastructure.Data.Config
                 .HasForeignKey(i => i.ClinicId)
                 .OnDelete(DeleteBehavior.Restrict);
 
+            builder.HasMany(c => c.ClinicWorkingHours)
+                .WithOne(cwh => cwh.Clinic)
+                .HasForeignKey(cwh => cwh.ClinicId)
+                .OnDelete(DeleteBehavior.Cascade);
+
 
         }
     }
diff --git a/ClinicFlow.Infrastructure/Data/Config/PatientConfiguration.cs b/ClinicFlow.Infrastructure/Data/Config/PatientConfiguration.cs
index e8f55e3..2e7ce6a 100644
--- a/ClinicFlow.Infrastructure/Data/Config/PatientConfiguration.cs
+++ b/ClinicFlow.Infrastructure/Data/Config/PatientConfiguration.cs
@@ -12,34 +12,28 @@ namespace ClinicFlow.Infrastructure.Data.Config
 
             builder.HasKey(p => p.Id);
 
-            builder.Property(p => p.FirstName)
-                .IsRequired()
-                .HasMaxLength(100);
+            builder.Property(p => p.PersonId)
+                .IsRequired();
 
-            builder.Property(p => p.LastName)
-                .IsRequired()
-                .HasMaxLength(100);
+            builder.Property(p => p.DateOfBirth)
+                .IsRequired();
 
-            builder.Property(p => p.PhoneNumber)
-                .IsRequired()
-                .HasMaxLength(20);
-
-            builder.Property(p => p.Email)
-                .HasMaxLength(255);
+            builder.Property(p => p.Gender)
+                .IsRequired();
 
             builder.Property(p => p.Notes)
                 .IsRequired(false)
-               .HasMaxLength(500);
-
-            builder.Property(p => p.Gender).IsRequired();
+                .HasMaxLength(500);
 
             builder.Property(p => p.Address)
                 .HasMaxLength(300);
 
-            builder.Property(p => p.BloodType).IsRequired(false);
+            builder.Property(p => p.BloodType)
+                .IsRequired(false);
 
-            builder.Property(p => p.NationalId).IsRequired(false).HasMaxLength(14);
-               
+            builder.Property(p => p.NationalId)
+                .IsRequired(false)
+                .HasMaxLength(14);
 
             builder.Property(p => p.EmergencyContactName)
                 .HasMaxLength(200);
@@ -50,33 +44,24 @@ namespace ClinicFlow.Infrastructure.Data.Config
             builder.Property(p => p.CreatedAt)
                 .HasDefaultValueSql("GETUTCDATE()");
 
-            builder.HasIndex(p => p.ClinicId);
-
-            builder.HasIndex(p => p.PhoneNumber).IsUnique(false);
+            builder.Property(p => p.UpdatedAt);
 
-            builder.HasIndex(p => p.Email).IsUnique(false);
+            builder.HasIndex(p => p.PersonId);
 
             builder.HasIndex(p => p.Gender);
 
-            builder.HasIndex(p => p.FirstName);
-
-            builder.HasIndex(p => p.LastName);
-
-            builder.HasIndex(p => new
-            {
-                p.FirstName,
-                p.LastName
-            });
-
             builder.HasIndex(p => p.DateOfBirth);
 
-            builder.HasIndex(p => p.Gender);
-
             builder.HasIndex(p => p.CreatedAt);
 
-            builder.HasOne(p => p.Clinic)
-                .WithMany(c => c.Patients)
-                .HasForeignKey(p => p.ClinicId)
+            builder.HasOne(p => p.Person)
+                .WithOne(per => per.Patient)
+                .HasForeignKey<Patient>(p => p.PersonId)
+                .OnDelete(DeleteBehavior.Restrict);
+
+            builder.HasMany(p => p.ClinicPatients)
+                .WithOne(cp => cp.Patient)
+                .HasForeignKey(cp => cp.PatientId)
                 .OnDelete(DeleteBehavior.Restrict);
 
             builder.HasMany(p => p.Appointments)
diff --git a/ClinicFlow.Infrastructure/Data/Config/UserConfiguration.cs b/ClinicFlow.Infrastructure/Data/Config/UserConfiguration.cs
index 94b5a76..a122360 100644
--- a/ClinicFlow.Infrastructure/Data/Config/UserConfiguration.cs
+++ b/ClinicFlow.Infrastructure/Data/Config/UserConfiguration.cs
@@ -12,13 +12,11 @@ namespace ClinicFlow.Infrastructure.Data.Config
 
             builder.HasKey(u => u.Id);
 
-            builder.Property(u => u.FirstName)
-                .IsRequired()
-                .HasMaxLength(100);
+            builder.Property(u => u.PersonId)
+                .IsRequired();
 
-            builder.Property(u => u.LastName)
-                .IsRequired()
-                .HasMaxLength(100);
+            builder.Property(u => u.ClinicId)
+                .IsRequired();
 
             builder.Property(u => u.Email)
                 .IsRequired()
@@ -32,14 +30,16 @@ namespace ClinicFlow.Infrastructure.Data.Config
                 .IsRequired()
                 .HasMaxLength(20);
 
-          
-
             builder.Property(u => u.IsActive)
                 .HasDefaultValue(true);
 
             builder.Property(u => u.CreatedAt)
                 .HasDefaultValueSql("GETUTCDATE()");
 
+            builder.Property(u => u.UpdatedAt);
+
+            builder.HasIndex(u => u.PersonId);
+
             builder.HasIndex(u => u.ClinicId);
 
             builder.HasIndex(u => u.Email)
@@ -48,16 +48,15 @@ namespace ClinicFlow.Infrastructure.Data.Config
             builder.HasIndex(u => u.PhoneNumber)
                 .IsUnique();
 
-            builder.HasIndex(u => u.FirstName);
-
-            builder.HasIndex(u => u.LastName);
-
-            builder.HasIndex(u => new { u.FirstName, u.LastName });
-
             builder.HasIndex(u => u.IsActive);
 
             builder.HasIndex(u => u.CreatedAt);
 
+            builder.HasOne(u => u.Person)
+                .WithOne(p => p.User)
+                .HasForeignKey<User>(u => u.PersonId)
+                .OnDelete(DeleteBehavior.Restrict);
+
             builder.HasOne(u => u.Clinic)
                 .WithMany(c => c.Users)
                 .HasForeignKey(u => u.ClinicId)
@@ -75,7 +74,13 @@ namespace ClinicFlow.Infrastructure.Data.Config
 
             builder.HasMany(r => r.UserRoles)
                  .WithOne(u => u.User)
-                 .HasForeignKey(r => r.RoleId);
+                 .HasForeignKey(r => r.UserId)
+                 .OnDelete(DeleteBehavior.Cascade);
+
+            builder.HasMany(u => u.RefreshTokens)
+                .WithOne(rt => rt.User)
+                .HasForeignKey(rt => rt.UserId)
+                .OnDelete(DeleteBehavior.Cascade);
         }
     }
 
